import React, { useState } from 'react'
import { Link } from 'react-router-dom'
import './auctions.scss'

export const BaseGallery = ({ thumbnail, title, clickUrl }) => {
  return (
    // <Link to={clickUrl} className="gallery-item">
    //   <img src={thumbnail} />
    //   <p className="title">{title}</p>
    // </Link>
  )
}
